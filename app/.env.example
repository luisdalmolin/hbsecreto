# Copy this file to .env.local and set the address reachable from your device or simulator.
# On a physical device, do not use localhost: use your computer's LAN IP address instead.
EXPO_PUBLIC_API_URL=http://localhost:8000

# Optional outside EAS builds. EAS-linked builds expose this automatically.
EXPO_PUBLIC_EAS_PROJECT_ID=
