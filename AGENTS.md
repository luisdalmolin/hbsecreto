# CPX Secreto

Secret Santa (Amigo Secreto) monorepo with the Laravel backend at (`backend`) and the Expo React Native mobile app at (`app`).

## Project Overview

See docs/project-overview.md.

## General

- The code and comments of this app should be in english.
- All user-facing content should use i18n and use Brazilian Portuguese as the default language.